const db = require('../../models')

module.exports = function(router) {
    router.get('/jobs/listings/:filter?', function(req, res) {
        
    });

    router.get('/jobs/create', function(req, res) {
        
    });
}