<nav class="navbar" role="navigation" aria-label="main navigation">
            <div class="navbar-brand">
                <a class="navbar-item" href="https://bulma.io">
                    <img src="https://bulma.io/images/bulma-logo.png" width="112" height="28">
                </a>

                <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false"
                    data-target="navbarBasicExample">
                    <span aria-hidden="true"></span>
                    <span aria-hidden="true"></span>
                    <span aria-hidden="true"></span>
                </a>
            </div>

            <div id="navbarBasicExample" class="navbar-menu">
            
                <div class="navbar-end">
                    <div class="navbar-item">
                        <div class="buttons">
                            <a class="button is-primary">
                                <strong>Profile</strong>
                            </a>
                            <a class="button is-light">
                                Log in
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </nav>

<div class="container">

    <div class= "card">

        <div class= "card-header-title">
             <h1>{{job.title}}</h1>
        </div>

        <p>Company: {{company.company_name}}</p>

        <p>{{job.description}}</p>

        <p>Type: {{job.type}}</p>

        <p>Wage: {{job.wage}}</p>

        <h2>Contact Information</h2>

        <p id="manager-name">Manager: {{manager.first_name}} {{manager.last_name}}</p>

        <p id="manager-email">E-mail: {{manager.email}}</p>

        <p id="manager-phone">Phone: {{manager.phone}}</p>

        <button class= "button">Contact Manager</button>

    </div>

</div>