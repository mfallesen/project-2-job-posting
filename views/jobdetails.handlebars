<nav class="navbar" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
        <a class="navbar-item" href="/">
            <img src="/images/Logo.png" width="112" height="28">
        </a>

        <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false"
            data-target="navbarBasicExample">
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
        </a>
    </div>

    <div id="navbarBasicExample" class="navbar-menu">

        <div class="navbar-end">
            <div class="navbar-item">
                        <div class="buttons">
                            {{#if manager.isLoggedIn}}
                            <a class="button is-dark" href='/manager/{{manager.id}}'>
                                <strong>Profile</strong>
                            </a>
                            <a class="button is-dark" href='/logout'>
                                <strong>Log Out</strong>
                            </a>
                            {{else}}
                            <a class="button is-dark navbar-login">
                                <strong>Log In</strong>
                            </a>
                            {{/if}}
                        </div>
                    </div>
        </div>
    </div>
</nav>

<div class="container">

    <div id="job-detail-card" class="card">

        <div id="job-title" class="card-header-title">
            <h1><strong>{{job.title}}</strong></h1>
        </div>
        <div id="job-info">
            <p><strong> Company:</strong> <span>{{company.company_name}}</span></p>
    <p><strong> They are looking for:</strong></p>
            <p> <span>{{job.description}}</span></p>

            <p> <strong>Type of work:</strong> <span>{{job.type}}</span></p>

            <p> <strong>Compensation:</strong> <span>${{job.wage}} an hour</span></p>

            <p><strong>Job Benefits:</strong></p>
                <ul class='job-benefits'>
            {{#each benefits}}
            <li>{{benefit_text}}</li>
            {{/each}}
        </ul>
        </div>

        

        <hr>
        <div id="job-manager">
            <h2><strong>Contact Information</strong></h2>
            <p>Want more details or to be considered for this position? Get in touch below!</p>

            <p><strong>Manager:</strong> <span id="manager-name-first">{{manager.first_name}}</span> <span
                    id="manager-name-last">{{manager.last_name}}</span></p>

            <p><strong>E-mail:</strong> <span id="manager-email">{{manager.email}}</span></p>

            <p><strong>Phone:</strong> <span id="manager-phone">{{manager.phone}}</span></p>
            <div class="buttons">
                 <button class="button details-button is-link contact-manager">Contact Manager</button>
                <button class="button details-button is-link back-btn">Back to Job Listings</button>
            </div>
           
        </div>
    </div>

    <div class="modal" id="contact-modal">
        <div class="modal-background"></div>
        <div class="modal-card">
        <header class="modal-card-head">
            <p class="modal-card-title">Contact Manager</p>
        </header>
        <div class="modal-card-body">
        <div id="contact-form" class="modal-content">
            <div class="control form-subtitle">
                 <p>Enter your name, e-mail, and other relevant information. We'll pass them along to the manager and let them know you're interested!</p>
            </div>
            <form action="" method="post" autocomplete="on">
                <div class="field">
                    <div class="control">
                        <label class="label">Name:</label>
                        <input id="contact-name" class="input" type="text" placeholder="Your name">
                    </div>
                </div>
                <div class="field">
                    <label class="label">Email:</label>
                    <div class="control has-icons-left has-icons-right">
                        <input id="contact-email" class="input" type="email" placeholder="Your e-mail address">
                        <span class="icon is-small is-left">
                            <i class="fas fa-envelope"></i>
                        </span>

                    </div>
                </div>

                <div class="field">
                    <label class="label">Other Information:</label>
                    <div class="control">
                        <textarea id="contact-message" class="textarea" placeholder="Ready for your Next Step?"></textarea>
                    </div>
                </div>

                <div class="field is-grouped">
                    <div class="control">
                        <button id="contact-send" class="button is-link" type="submit">Submit</button>
                        
                    </div>
                    <div class="control">
                        <button id= "contact-close" class="button is-link is-light">Cancel</button>
                    </div>
                    <p id="email-sent" class="sent"> </p>
                </div>
            </form>
        </div>
        <button class="modal-close is-large" aria-label="close"></button>
    </div>
    </div>
    </div>

</div>

<div class="modal" id="login-modal">
    <div class="modal-background"></div>
    <div class="modal-card">
        <header class="modal-card-head">
            <p class="modal-card-title">Log In</p>
            <button class="delete" aria-label="close"></button>
        </header>
        <section class="modal-card-body">

            <div class="field is-horizontal">
                <div class="field-label is-normal">
                    <label class="label">E-mail</label>
                </div>
                <div class="field-body">
                    <div class="field is-expanded">
                        <p class="control is-expanded has-icons-left has-icons-right">
                            <input class="input loginEmail" type="email" placeholder="E-mail address">
                            <span class="icon is-small is-left">
                                <i class="fas fa-envelope"></i>
                            </span>
                        </p>
                    </div>
                </div>
            </div>

            <div class="field is-horizontal">
                <div class="field-label is-normal">
                    <label class="label">Password</label>
                </div>
                <div class="field-body">
                    <div class="field is-expanded">
                        <p class="control has-icons-left">
                            <input class="input loginPassword" type="password" placeholder="Password">
                            <span class="icon is-small is-left">
                                <i class="fas fa-lock"></i>
                            </span>
                        </p>
                    </div>
                </div>
            </div>



        </section>
        <footer class="modal-card-foot">
            <button class="button is-primary loginBtn" class="submit">
                Submit
            </button>

        </footer>
    </div>
</div>